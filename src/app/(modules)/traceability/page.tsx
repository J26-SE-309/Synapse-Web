import type { Metadata } from "next";

import { ModulePlaceholder } from "@/shared/components/ModulePlaceholder";

export const metadata: Metadata = { title: "Traceability" };

// Owned by @dinuwa2500. Replace the placeholder with this module's pages and keep the module's
// components, hooks and API calls in private folders here (e.g. _components/, _lib/).
export default function TraceabilityPage() {
  return <ModulePlaceholder slug="traceability" />;
}

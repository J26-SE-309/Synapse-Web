import type { Metadata } from "next";

import { ModulePlaceholder } from "@/shared/components/ModulePlaceholder";

export const metadata: Metadata = { title: "Requirement Quality" };

// Owned by @AmaLiyanage. Replace the placeholder with this module's pages and keep the module's
// components, hooks and API calls in private folders here (e.g. _components/, _lib/).
export default function RequirementQualityPage() {
  return <ModulePlaceholder slug="requirement-quality" />;
}

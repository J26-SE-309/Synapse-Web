# Copy to .env.local to override. Never commit .env.local.

# Where the browser reaches the API gateway (gateway/). Every call from the web app goes through it.
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000
